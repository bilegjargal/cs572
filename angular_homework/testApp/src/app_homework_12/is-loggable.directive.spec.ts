import { IsLoggableDirective } from './is-loggable.directive';

describe('IsLoggableDirective', () => {
  it('should create an instance', () => {
    const directive = new IsLoggableDirective();
    expect(directive).toBeTruthy();
  });
});
